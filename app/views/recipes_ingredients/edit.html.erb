<!-- Validation failure messages -->
<% if @recipes_ingredient.errors.any? %>
  <% @recipes_ingredient.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Recipes Ingredient #<%= @recipes_ingredient.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_recipes_ingredient/<%= @recipes_ingredient.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for recipe_id -->
      <div class="form-group">
        <label for="recipe_id" class="control-label">
          Recipe
        </label>

        <input type="text" id="recipe_id" name="recipe_id" placeholder="Recipe" class="form-control" value="<%= @recipes_ingredient.recipe_id %>">
      </div>

      <!-- Label and input for ingredient_id -->
      <div class="form-group">
        <label for="ingredient_id" class="control-label">
          Ingredient
        </label>

        <input type="text" id="ingredient_id" name="ingredient_id" placeholder="Ingredient" class="form-control" value="<%= @recipes_ingredient.ingredient_id %>">
      </div>

      <button class="btn btn-info">
        Update Recipes Ingredient
      </button>
      or
      <a href="/recipes_ingredients/<%= @recipes_ingredient.id %>">Cancel</a>
    </form>
  </div>
</div>
